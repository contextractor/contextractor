{
  "name": "@contextractor/apify",
  "version": "0.4.14",
  "private": true,
  "description": "Apify Actor that crawls websites and extracts main-content text. Built on Trafilatura Core and Crawlee.",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "deploy": "node scripts/deploy.mjs",
    "start": "node dist/main.js",
    "start:prod": "node dist/main.js"
  },
  "dependencies": {
    "@contextractor/crawler": "workspace:*",
    "@contextractor/schema": "workspace:*",
    "apify": "^3.7.2"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "typescript": "^6.0.3"
  }
}
