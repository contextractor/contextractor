# Bundled assets live under cli/ and are staged at build time by
# scripts/stage_vendor.py (gitignored). This file keeps _vendor importable as a
# package even in a source checkout where the assets have not been staged yet.
