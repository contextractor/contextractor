[project]
name = "contextractor-workspace"
version = "0.3.10"
license = "Apache-2.0"
requires-python = ">=3.12"

[tool.uv.workspace]
members = ["packages/*", "apps/*"]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
