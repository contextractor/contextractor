# Step: Sync Standalone README

## TLDR

Copy the updated root `README.md` to `apps/contextractor-standalone/README.md` to keep them identical.

## Instructions

After all changes to the root README are complete:

```bash
cp README.md apps/contextractor-standalone/README.md
```

Verify both files are identical:

```bash
diff README.md apps/contextractor-standalone/README.md
```
