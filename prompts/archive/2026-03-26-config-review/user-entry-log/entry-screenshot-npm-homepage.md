# Screenshot: npm package homepage change

The screenshot shows the npm package settings page for `contextractor` (v0.2.1).

Action required: Change the `homepage` field in `package.json` from `https://github.com/contextractor/contextractor` to `https://www.contextractor.com/` and publish a new version.
