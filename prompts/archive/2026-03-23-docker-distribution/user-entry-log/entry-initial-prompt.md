Create a public Docker image for contextractor standalone CLI. Users should be able to run:

docker run contextractor https://example.com

Add Docker build+publish to the existing release.yml GitHub Actions workflow. Document Docker usage in all docs.
