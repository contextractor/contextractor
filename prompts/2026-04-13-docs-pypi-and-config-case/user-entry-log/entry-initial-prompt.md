add to all the docs that there is also python package at https://pypi.org/project/contextractor/

there must not be used camel_case in any config files like for proxy rotation at https://www.npmjs.com/package/contextractor 
eamaple: `per_request` instead use camelCased `perRequest` (except this is the convention that is used in python ecosystem). do deep ressearch all is ok.

then run 
/Users/miroslavsekera/r/contextractor/.claude/commands/sync/docs.md
/Users/miroslavsekera/r/contextractor/.claude/commands/sync/gui.md
  then commit and push