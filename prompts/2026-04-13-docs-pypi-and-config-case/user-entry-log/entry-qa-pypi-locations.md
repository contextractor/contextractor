# Q&A: PyPI Link Locations

**Q:** The PyPI link is already in all 5 READMEs (from commit f5b1f37). Are there other docs where it should be added?

**A:** Yes — add to:
1. CLI `--help` output
2. Apify Actor description (input_schema.json or actor.json)
3. npm `package.json` fields
