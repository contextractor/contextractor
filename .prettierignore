# Prettier is scoped to Markdown only (Biome owns JS/TS/JSON/CSS).
# Paths here mirror biome.json's ignore list. node_modules, dist, target,
# storage, venv, .turbo etc. are already skipped via .gitignore.
.claude
prompts
media
temp
**/fixtures
**/test-suites
**/test-suites-output
packages/extraction/native/npm
